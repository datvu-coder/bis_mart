import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFFE05C27);
  static const primaryDark = Color(0xFFB84A1E);
  static const primaryLight = Color(0xFFFFF4EF);
  static const accent = Color(0xFFFF8A50);

  // Surfaces
  static const background = Color(0xFFF5F6FA);
  static const cardBg = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF0F2F7);
  static const surfaceElevated = Color(0xFFFFFFFF);

  // Text
  static const textDark = Color(0xFF111827);
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF4B5563);
  static const textGrey = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);

  // Borders & dividers
  static const border = Color(0xFFE5E7EB);
  static const borderLight = Color(0xFFF3F4F6);
  static const divider = Color(0xFFF0F0F0);

  // Status
  static const success = Color(0xFF059669);
  static const successLight = Color(0xFFECFDF5);
  static const warning = Color(0xFFD97706);
  static const warningLight = Color(0xFFFFFBEB);
  static const error = Color(0xFFDC2626);
  static const errorLight = Color(0xFFFEF2F2);
  static const info = Color(0xFF2563EB);
  static const infoLight = Color(0xFFEFF6FF);

  // Misc
  static const white = Color(0xFFFFFFFF);

  // Sidebar
  static const sidebarBg = Color(0xFF0F172A);
  static const sidebarSurface = Color(0xFF1E293B);
  static const sidebarText = Color(0xFF94A3B8);
  static const sidebarActive = Color(0xFFFFFFFF);

  // Gradients
  static const gradientStart = Color(0xFFE05C27);
  static const gradientEnd = Color(0xFFFF8A50);
  static const gradientDarkStart = Color(0xFF0F172A);
  static const gradientDarkEnd = Color(0xFF1E293B);

  // Shadows
  static const shadow = Color(0x0A000000);
  static const shadowMedium = Color(0x14000000);

  // Purple — dùng cho Phân quyền (admin)
  static const purpleAccent = Color(0xFF7C3AED);
  static const purpleLight  = Color(0xFFF5F3FF);
}
